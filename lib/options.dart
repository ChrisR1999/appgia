import 'package:flutter/material.dart';

class SettingsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp();
  }

  MaterialApp _settingsScreenUI() {
    return MaterialApp();
  }

  Widget _buildAppBar() {
    return AppBar();
  }
}
